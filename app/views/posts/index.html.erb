<%= render "/current_user_heding" %>
<% @posts.each do |post| %>
  <h2 class='post_heading'>
    <a href='/users/<%= @user.id %>/posts/<%= post.id %>'>Post <%= post.id %></a>
  </h2>
  <ul class='flex_block_row'>
    <li>
      <h3><%= post.title %></h3>
      <p><%= post.text%></p>
    </li>
    <li>Likes:<%= post.likes_counter%></li>
    <li>Comments:<%= post.comments_counter%></li>
  </ul>
  <h3 class='comments_heading'>Comments:</h3>
  <% post.show_5_comments.each do |comment| %>
    <ul class='comment_list'>
      <li><%= comment.text %></li>
      <li>Added on:<%= comment.created_at%></li>
    </ul>
  <% end %>
<% end %>
